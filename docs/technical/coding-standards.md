# Coding Standards

Use TypeScript strict mode. Keep route files thin. Server components fetch initial page data. Client components handle forms, mutations, polling, tabs, filters, and live states. API writes must validate DTOs and write audit logs for sensitive actions.
