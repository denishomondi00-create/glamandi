import { LoadingState } from "@/components/feedback/loading-state";

export default function Loading() {
  return <LoadingState title="Loading Glamandi Control Center" description="Preparing a clean operational view." />;
}
